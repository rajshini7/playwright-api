{
  "name": "api-automation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:api": "playwright test tests/api",
    "test": "npm run test:api",
    "newman:run": "npx newman run postman/CRUD_VAL.postman_collection.json --env-var base_url=https://jsonplaceholder.typicode.com/posts --reporters cli,htmlextra --reporter-htmlextra-export reports/newman/newman-report.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^25.0.9",
    "dotenv": "^16.4.1",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
